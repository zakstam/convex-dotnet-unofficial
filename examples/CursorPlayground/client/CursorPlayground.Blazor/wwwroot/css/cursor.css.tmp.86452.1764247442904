/* Cursor Playground Styles - Enhanced UI */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
    --glass-bg: rgba(255, 255, 255, 0.08);
    --glass-border: rgba(255, 255, 255, 0.15);
    --glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    --accent-blue: #64C8FF;
    --accent-pink: #f093fb;
    --accent-purple: #764ba2;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    overflow: hidden;
    background: #1a1a2e;
}

/* Animated Gradient Background */
.cursor-playground {
    width: 100vw;
    height: 100vh;
    position: relative;
    overflow: hidden;
    background: linear-gradient(-45deg, #1a1a2e, #16213e, #0f3460, #533483, #764ba2, #667eea);
    background-size: 400% 400%;
    animation: gradientShift 15s ease infinite;
}

@keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

/* Ambient Particle Layer */
.cursor-playground::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
        radial-gradient(2px 2px at 20px 30px, rgba(255,255,255,0.3), transparent),
        radial-gradient(2px 2px at 40px 70px, rgba(255,255,255,0.2), transparent),
        radial-gradient(2px 2px at 50px 160px, rgba(255,255,255,0.3), transparent),
        radial-gradient(2px 2px at 90px 40px, rgba(255,255,255,0.2), transparent),
        radial-gradient(2px 2px at 130px 80px, rgba(255,255,255,0.3), transparent),
        radial-gradient(2px 2px at 160px 120px, rgba(255,255,255,0.2), transparent),
        radial-gradient(3px 3px at 200px 50px, rgba(100,200,255,0.4), transparent),
        radial-gradient(2px 2px at 250px 90px, rgba(255,255,255,0.2), transparent),
        radial-gradient(2px 2px at 300px 150px, rgba(255,255,255,0.3), transparent),
        radial-gradient(3px 3px at 350px 30px, rgba(240,147,251,0.4), transparent),
        radial-gradient(2px 2px at 400px 100px, rgba(255,255,255,0.2), transparent),
        radial-gradient(2px 2px at 450px 180px, rgba(255,255,255,0.3), transparent);
    background-size: 500px 200px;
    animation: starFloat 60s linear infinite;
    pointer-events: none;
    z-index: 0;
}

@keyframes starFloat {
    from { background-position: 0 0; }
    to { background-position: 500px 200px; }
}

/* Glowing orbs in background */
.cursor-playground::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background:
        radial-gradient(circle at 20% 80%, rgba(100,200,255,0.15) 0%, transparent 30%),
        radial-gradient(circle at 80% 20%, rgba(240,147,251,0.15) 0%, transparent 30%),
        radial-gradient(circle at 50% 50%, rgba(118,75,162,0.1) 0%, transparent 50%);
    pointer-events: none;
    z-index: 0;
    animation: orbPulse 8s ease-in-out infinite;
}

@keyframes orbPulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.6; }
}

/* Constellation canvas (background layer) */
.constellation-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
}

/* Content layer */
.playground-content {
    position: relative;
    z-index: 1001;
    width: 100%;
    height: 100%;
    pointer-events: none;
}

.playground-content > * {
    pointer-events: auto;
}

/* Remote cursor styling - Enhanced */
.remote-cursor {
    position: fixed;
    top: 0;
    left: 0;
    pointer-events: none;
    z-index: 1000;
    will-change: transform, opacity;
}

.cursor-pointer {
    width: 14px;
    height: 14px;
    border-radius: 50%;
    background-color: var(--accent-blue);
    box-shadow:
        0 0 10px currentColor,
        0 0 20px currentColor,
        0 0 40px currentColor,
        inset 0 0 5px rgba(255,255,255,0.5);
    position: relative;
    animation: cursorPulse 2s ease-in-out infinite;
}

.cursor-pointer::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 4px;
    height: 4px;
    background: white;
    border-radius: 50%;
    transform: translate(-50%, -50%);
}

@keyframes cursorPulse {
    0%, 100% {
        transform: scale(1);
        box-shadow:
            0 0 10px currentColor,
            0 0 20px currentColor,
            0 0 40px currentColor;
    }
    50% {
        transform: scale(1.1);
        box-shadow:
            0 0 15px currentColor,
            0 0 30px currentColor,
            0 0 60px currentColor;
    }
}

.cursor-label {
    position: absolute;
    top: 18px;
    left: 18px;
    background: var(--glass-bg);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    color: white;
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 500;
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 6px;
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
}

.cursor-emoji {
    font-size: 14px;
}

.cursor-name {
    font-weight: 600;
    letter-spacing: 0.3px;
}

/* Cursor trail effect */
.cursor-trail {
    position: fixed;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.6);
    pointer-events: none;
    animation: trail-fade 0.5s ease-out forwards;
}

@keyframes trail-fade {
    to {
        opacity: 0;
        transform: scale(0.5);
    }
}

/* Ripple effect on click */
.ripple-effect {
    position: fixed;
    border: 2px solid rgba(255, 255, 255, 0.8);
    border-radius: 50%;
    pointer-events: none;
    animation: ripple-expand 1s ease-out forwards;
}

@keyframes ripple-expand {
    0% {
        width: 0;
        height: 0;
        opacity: 1;
    }
    100% {
        width: 200px;
        height: 200px;
        opacity: 0;
    }
}

/* Reaction emoji */
.reaction-emoji {
    position: fixed;
    font-size: 32px;
    pointer-events: none;
    z-index: 999;
    animation: reaction-bounce 1s ease-out forwards;
}

@keyframes reaction-bounce {
    0% {
        transform: scale(0) translateY(0);
        opacity: 1;
    }
    50% {
        transform: scale(1.2) translateY(-20px);
    }
    100% {
        transform: scale(1) translateY(-40px);
        opacity: 0;
    }
}

/* Header - Enhanced */
.playground-header {
    position: absolute;
    top: 24px;
    left: 24px;
    z-index: 100;
    color: white;
}

.playground-title {
    font-size: 36px;
    font-weight: 800;
    background: linear-gradient(135deg, #fff 0%, var(--accent-blue) 50%, var(--accent-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: none;
    margin-bottom: 8px;
    letter-spacing: -0.5px;
}

.playground-subtitle {
    font-size: 15px;
    opacity: 0.8;
    font-weight: 400;
    letter-spacing: 0.3px;
}

/* User count badge - Enhanced */
.user-count {
    position: absolute;
    top: 24px;
    right: 24px;
    background: var(--glass-bg);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    color: white;
    padding: 14px 24px;
    border-radius: 50px;
    font-size: 15px;
    font-weight: 600;
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    z-index: 100;
    display: flex;
    align-items: center;
    gap: 8px;
}

.user-count-number {
    background: linear-gradient(135deg, var(--accent-blue), var(--accent-pink));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: 22px;
    font-weight: 700;
}

/* Instructions - Enhanced */
.instructions {
    position: absolute;
    bottom: 24px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--glass-bg);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    color: white;
    padding: 16px 28px;
    border-radius: 20px;
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    z-index: 50;
    text-align: center;
    opacity: 0.7;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.instructions:hover {
    opacity: 1;
    transform: translateX(-50%) translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
}

.instructions p {
    margin: 6px 0;
    font-size: 13px;
    opacity: 0.9;
    letter-spacing: 0.2px;
}

.instructions strong {
    color: var(--accent-blue);
    font-weight: 600;
}

/* User setup modal - Enhanced */
.setup-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    pointer-events: none;
    transition: background-color 0.4s ease;
}

.setup-modal.visible {
    background: rgba(10, 10, 30, 0.9);
    pointer-events: all;
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
}

.setup-content {
    background: linear-gradient(145deg, rgba(255,255,255,0.95), rgba(245,245,255,0.98));
    padding: 32px 36px;
    border-radius: 28px;
    box-shadow:
        0 25px 80px rgba(0, 0, 0, 0.5),
        0 0 0 1px rgba(255,255,255,0.2),
        inset 0 1px 0 rgba(255,255,255,0.8);
    max-width: 480px;
    width: 90%;
    transform: scale(0.9) translateY(20px);
    opacity: 0;
    transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.setup-modal.visible .setup-content {
    transform: scale(1) translateY(0);
    opacity: 1;
}

.setup-header {
    text-align: center;
    margin-bottom: 24px;
}

.setup-title {
    font-size: 28px;
    font-weight: 800;
    margin-bottom: 6px;
    background: linear-gradient(135deg, #667eea, #764ba2, #f093fb);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.setup-subtitle {
    font-size: 14px;
    color: #666;
    font-weight: 400;
}

.setup-footer {
    margin-top: 20px;
    padding-top: 16px;
    border-top: 1px solid rgba(102, 126, 234, 0.15);
    text-align: center;
    font-size: 11px;
    color: #999;
    letter-spacing: 0.3px;
}

.form-group {
    margin-bottom: 20px;
}

.form-group label {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 8px;
    font-weight: 700;
    font-size: 11px;
    color: #444;
    text-transform: uppercase;
    letter-spacing: 0.8px;
}

.label-icon {
    font-size: 14px;
    transition: transform 0.3s ease;
}

.form-group:hover .label-icon {
    transform: scale(1.15);
}

.input-hint {
    margin-top: 6px;
    font-size: 10px;
    color: #999;
    text-align: right;
}

.form-input {
    width: 100%;
    padding: 12px 16px;
    border: 2px solid #e8e8f0;
    border-radius: 12px;
    font-size: 15px;
    font-family: inherit;
    transition: all 0.3s ease;
    background: white;
}

.form-input:focus {
    outline: none;
    border-color: #667eea;
    box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.15);
}

.form-input::placeholder {
    color: #aaa;
}

/* Emoji Grid - Enhanced */
.emoji-grid {
    display: grid;
    grid-template-columns: repeat(8, 1fr);
    gap: 6px;
}

.emoji-button {
    font-size: 22px;
    padding: 8px;
    border: 2px solid transparent;
    border-radius: 12px;
    background: #f8f9ff;
    cursor: pointer;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    text-align: center;
}

.emoji-button:hover {
    background: #eef0ff;
    transform: scale(1.1);
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.2);
}

.emoji-button.selected {
    border-color: #667eea;
    background: linear-gradient(135deg, #f0f0ff, #e8e8ff);
    transform: scale(1.1);
    box-shadow: 0 4px 16px rgba(102, 126, 234, 0.3);
}

/* Color Grid - Enhanced */
.color-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 10px;
}

.color-button {
    width: 100%;
    aspect-ratio: 1;
    border: 3px solid transparent;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 16px;
    background-color: var(--color-value);
}

.color-button:hover {
    transform: scale(1.08);
    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.25);
}

.color-button.selected {
    border-color: white;
    transform: scale(1.08);
    box-shadow:
        0 0 0 3px rgba(0, 0, 0, 0.15),
        0 6px 20px rgba(0, 0, 0, 0.3);
}

.checkmark {
    color: white;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.5);
    font-weight: bold;
    font-size: 18px;
}

/* Preview Section - Enhanced */
.preview-section {
    margin: 20px 0;
    padding: 16px;
    background: linear-gradient(135deg, #f5f7ff 0%, #e9ecff 100%);
    border-radius: 16px;
    text-align: center;
    border: 1px solid rgba(102, 126, 234, 0.1);
}

.preview-label {
    font-size: 10px;
    color: #888;
    margin-bottom: 12px;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    font-weight: 600;
}

.user-preview {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    font-size: 20px;
}

.preview-card {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 12px 20px;
    background: white;
    border-radius: 14px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
}

.preview-cursor {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--cursor-color), color-mix(in srgb, var(--cursor-color) 70%, #fff));
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 0 16px color-mix(in srgb, var(--cursor-color) 40%, transparent);
    animation: previewPulse 2s ease-in-out infinite;
}

@keyframes previewPulse {
    0%, 100% {
        transform: scale(1);
        box-shadow: 0 0 20px color-mix(in srgb, var(--cursor-color) 40%, transparent);
    }
    50% {
        transform: scale(1.05);
        box-shadow: 0 0 30px color-mix(in srgb, var(--cursor-color) 50%, transparent);
    }
}

.preview-emoji {
    font-size: 20px;
    filter: drop-shadow(0 1px 2px rgba(0,0,0,0.2));
}

@keyframes previewBounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-4px); }
}

.preview-info {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 2px;
}

.preview-name {
    font-weight: 700;
    font-size: 16px;
}

.preview-status {
    font-size: 11px;
    color: #22c55e;
    display: flex;
    align-items: center;
    gap: 5px;
}

.preview-status::before {
    content: '';
    width: 6px;
    height: 6px;
    background: #22c55e;
    border-radius: 50%;
    animation: statusPulse 1.5s ease-in-out infinite;
}

@keyframes statusPulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
}

/* Submit Button - Enhanced */
.submit-button {
    width: 100%;
    padding: 14px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
    background-size: 200% 200%;
    color: white;
    border: none;
    border-radius: 14px;
    font-size: 15px;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.4s ease;
    box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
    letter-spacing: 0.5px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
}

.submit-button:hover:not(:disabled) {
    background-position: 100% 0;
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(102, 126, 234, 0.5);
}

.submit-button:hover:not(:disabled) .button-arrow {
    transform: translateX(4px);
}

.submit-button:active:not(:disabled) {
    transform: translateY(-1px);
}

.submit-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.submit-button.loading {
    opacity: 0.8;
}

.button-arrow {
    font-size: 20px;
    transition: transform 0.3s ease;
}

.button-spinner {
    width: 18px;
    height: 18px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-top-color: white;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

/* Error Message */
.error-message {
    margin-top: 20px;
    padding: 14px 16px;
    background: linear-gradient(135deg, #fff5f5, #fee);
    color: #c53030;
    border-radius: 12px;
    font-size: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    border: 1px solid rgba(197, 48, 48, 0.2);
}

.error-icon {
    width: 20px;
    height: 20px;
    background: #c53030;
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    font-weight: bold;
    flex-shrink: 0;
}

/* Stats panel - Enhanced */
.stats-panel {
    position: absolute;
    bottom: 24px;
    right: 24px;
    background: var(--glass-bg);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    color: white;
    padding: 18px 22px;
    border-radius: 20px;
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    z-index: 100;
    font-size: 13px;
    min-width: 160px;
}

.stats-panel .stat-item {
    display: flex;
    justify-content: space-between;
    margin: 6px 0;
    align-items: center;
}

.stats-panel .stat-label {
    opacity: 0.7;
    font-weight: 500;
}

.stats-panel .stat-value {
    font-weight: 700;
    background: linear-gradient(135deg, var(--accent-blue), var(--accent-pink));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-family: 'SF Mono', 'Consolas', 'Monaco', monospace;
}

/* Settings Panel - Enhanced */
.settings-panel {
    position: absolute;
    bottom: 90px;
    left: 24px;
    color: white;
    z-index: 50;
}

.settings-header {
    display: inline-flex;
    gap: 8px;
    align-items: center;
    padding: 10px 16px;
    cursor: pointer;
    font-weight: 600;
    font-size: 12px;
    user-select: none;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    border-radius: 20px;
    background: var(--glass-bg);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid var(--glass-border);
    opacity: 0.5;
}

.settings-header:hover {
    background: rgba(255, 255, 255, 0.15);
    opacity: 1;
    transform: scale(1.05);
    box-shadow: var(--glass-shadow);
}

.toggle-icon {
    opacity: 0.6;
    font-size: 10px;
}

.settings-content {
    padding: 20px;
    animation: slideUp 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-radius: 20px;
    border: 1px solid var(--glass-border);
    margin-top: 12px;
    box-shadow: var(--glass-shadow);
    width: 320px;
    position: relative;
    z-index: 101;
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.setting-item {
    margin-bottom: 24px;
}

.setting-item:last-child {
    margin-bottom: 0;
}

.setting-item label {
    display: flex;
    justify-content: space-between;
    margin-bottom: 10px;
    font-size: 13px;
}

.setting-label {
    opacity: 0.8;
    font-weight: 600;
}

.setting-value {
    background: linear-gradient(135deg, var(--accent-blue), var(--accent-pink));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 700;
    font-family: 'SF Mono', 'Consolas', 'Monaco', monospace;
}

.setting-slider {
    width: 100%;
    height: 6px;
    border-radius: 3px;
    background: rgba(255, 255, 255, 0.15);
    outline: none;
    appearance: none;
    cursor: pointer;
    transition: background 0.2s ease;
}

.setting-slider:hover {
    background: rgba(255, 255, 255, 0.25);
}

.setting-slider::-webkit-slider-thumb {
    appearance: none;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--accent-blue), var(--accent-pink));
    cursor: pointer;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.setting-slider::-webkit-slider-thumb:hover {
    transform: scale(1.2);
    box-shadow: 0 4px 12px rgba(100, 200, 255, 0.5);
}

.setting-slider::-moz-range-thumb {
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--accent-blue), var(--accent-pink));
    cursor: pointer;
    border: none;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
}

.setting-hint {
    font-size: 11px;
    opacity: 0.5;
    margin-top: 6px;
    font-style: italic;
}

.reset-button {
    width: 100%;
    padding: 12px;
    background: rgba(255, 255, 255, 0.08);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 12px;
    cursor: pointer;
    font-size: 13px;
    font-weight: 600;
    transition: all 0.3s ease;
    margin-top: 16px;
}

.reset-button:hover {
    background: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.3);
    transform: translateY(-2px);
}

/* Loading Screen - Enhanced */
.loading-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(-45deg, #1a1a2e, #16213e, #0f3460, #533483);
    background-size: 400% 400%;
    animation: gradientShift 15s ease infinite;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10000;
}

.loading-content {
    text-align: center;
    color: white;
}

.loading-content h1 {
    font-size: 48px;
    font-weight: 800;
    background: linear-gradient(135deg, #fff, var(--accent-blue), var(--accent-pink));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 16px;
    animation: titlePulse 2s ease-in-out infinite;
}

@keyframes titlePulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.8; transform: scale(1.02); }
}

.loading-content p {
    font-size: 18px;
    opacity: 0.7;
    margin-bottom: 32px;
    font-weight: 400;
}

.loading-spinner {
    display: flex;
    justify-content: center;
    gap: 8px;
}

.loading-spinner::before,
.loading-spinner::after {
    content: '';
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--accent-blue);
    animation: loadingBounce 1.4s ease-in-out infinite;
}

.loading-spinner::after {
    background: var(--accent-pink);
    animation-delay: 0.2s;
}

@keyframes loadingBounce {
    0%, 80%, 100% { transform: scale(0.6); opacity: 0.5; }
    40% { transform: scale(1); opacity: 1; }
}

/* Reaction Picker - Enhanced */
.reaction-picker {
    position: fixed;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    padding: 10px;
    border-radius: 24px;
    border: 1px solid var(--glass-border);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.5);
    display: flex;
    gap: 6px;
    z-index: 10000;
    transform: translate(-50%, -50%);
    animation: pickerPopIn 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}

@keyframes pickerPopIn {
    from {
        opacity: 0;
        transform: translate(-50%, -50%) scale(0.8);
    }
    to {
        opacity: 1;
        transform: translate(-50%, -50%) scale(1);
    }
}

.reaction-button {
    width: 52px;
    height: 52px;
    border: none;
    background: rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    font-size: 28px;
    cursor: pointer;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    justify-content: center;
}

.reaction-button:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: scale(1.2) translateY(-4px);
}

.reaction-button:active {
    transform: scale(0.95);
}

/* Keyboard Shortcuts Help - Enhanced */
.shortcuts-help {
    position: absolute;
    bottom: 90px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--glass-bg);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    color: white;
    padding: 12px 20px;
    border-radius: 16px;
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    z-index: 50;
    font-size: 12px;
    display: flex;
    gap: 20px;
    opacity: 0.4;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.shortcuts-help:hover {
    opacity: 1;
    transform: translateX(-50%) translateY(-4px);
}

.shortcut-item {
    display: flex;
    align-items: center;
    gap: 8px;
}

.shortcut-key {
    background: rgba(255, 255, 255, 0.15);
    padding: 4px 10px;
    border-radius: 8px;
    font-weight: 700;
    font-family: 'SF Mono', 'Consolas', monospace;
    font-size: 11px;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

/* User List Panel */
.user-list-panel {
    position: fixed;
    top: 80px;
    right: 24px;
    background: var(--glass-bg);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    color: white;
    border-radius: 20px;
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    z-index: 1002;
    min-width: 220px;
    max-height: 400px;
    overflow: hidden;
    transition: all 0.3s ease;
}

.user-list-panel.collapsed {
    min-width: auto;
}

.user-list-panel.collapsed .user-list-content {
    display: none;
}

.user-list-header {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 14px 16px;
    cursor: pointer;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    transition: background 0.2s ease;
}

.user-list-header:hover {
    background: rgba(255, 255, 255, 0.05);
}

.user-list-title {
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    flex: 1;
}

.user-list-count {
    background: linear-gradient(135deg, var(--accent-blue), var(--accent-pink));
    color: white;
    font-size: 11px;
    font-weight: 700;
    padding: 3px 8px;
    border-radius: 10px;
}

.user-list-content {
    padding: 12px 16px;
    max-height: 280px;
    overflow-y: auto;
}

.user-list-empty {
    text-align: center;
    padding: 20px;
    opacity: 0.5;
    font-size: 13px;
}

.user-list-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.user-list-item:last-child {
    border-bottom: none;
}

.user-list-avatar {
    font-size: 20px;
}

.user-list-name {
    font-weight: 500;
    font-size: 14px;
}

.user-list-indicator {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #68D391;
    margin-left: auto;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .playground-title {
        font-size: 28px;
    }

    .playground-header {
        top: 16px;
        left: 16px;
    }

    .user-count {
        top: 16px;
        right: 16px;
        padding: 10px 16px;
        font-size: 14px;
    }

    .instructions {
        padding: 14px 20px;
        bottom: 16px;
    }

    .instructions p {
        font-size: 12px;
    }

    .setup-content {
        padding: 32px 24px;
        max-height: 95vh;
    }

    .setup-title {
        font-size: 26px;
    }

    .emoji-grid {
        grid-template-columns: repeat(6, 1fr);
        gap: 8px;
    }

    .emoji-button {
        font-size: 22px;
        padding: 10px;
    }

    .color-grid {
        grid-template-columns: repeat(5, 1fr);
        gap: 10px;
    }

    .stats-panel {
        bottom: 16px;
        right: 16px;
        padding: 14px 18px;
    }

    .settings-panel {
        left: 16px;
        bottom: 70px;
    }

    .settings-content {
        width: 280px;
    }

    .shortcuts-help {
        flex-wrap: wrap;
        justify-content: center;
        gap: 12px;
        bottom: 70px;
        padding: 10px 16px;
    }

    .reaction-picker {
        padding: 8px;
    }

    .reaction-button {
        width: 44px;
        height: 44px;
        font-size: 24px;
    }

    .user-list-panel {
        display: none;
    }
}
