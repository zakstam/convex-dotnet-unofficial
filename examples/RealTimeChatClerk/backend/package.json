{
  "name": "realtime-chat-backend",
  "version": "1.0.0",
  "description": "Convex backend for real-time chat application",
  "scripts": {
    "dev": "convex dev",
    "build": "convex build",
    "deploy": "convex deploy"
  },
  "dependencies": {
    "@convex-dev/auth": "^0.0.90",
    "convex": "^1.9.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
