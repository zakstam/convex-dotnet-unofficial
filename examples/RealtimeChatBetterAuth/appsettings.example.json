{
  "_comment": "Copy this file to appsettings.json and configure your Convex deployment URLs. BetterAuth:SiteUrl must match backend SITE_URL (set via `npx convex env set SITE_URL ...`).",
  "Convex": {
    "DeploymentUrl": "https://your-deployment.convex.cloud",
    "InitialMessageLimit": 10,
    "EnableDebugLogging": false
  },
  "BetterAuth": {
    "SiteUrl": "https://your-deployment.convex.site"
  }
}
